import type { DataType, TableContext } from '../types';
declare const generateStatus: <Data extends DataType>({ tableProps, tableInstance }: TableContext<Data>) => string;
export default generateStatus;
