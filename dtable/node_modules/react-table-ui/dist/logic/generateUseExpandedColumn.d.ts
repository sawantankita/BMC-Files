import type { DataType, ReactTableUIProps, Hooks } from '../types';
declare const generateUseExpandedColumn: <Data extends DataType>(props: ReactTableUIProps<Data>) => {
    useExpandedColumn: (hooks: Hooks<Data>) => void;
    disableExpander: boolean;
};
export default generateUseExpandedColumn;
