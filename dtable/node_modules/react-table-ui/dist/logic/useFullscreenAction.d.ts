import * as React from 'react';
import type { DataType, TableContext } from '../types';
export default function useFullscreenAction<Data extends DataType>({ tableRef, tableProps }: TableContext<Data>): {
    title: string;
    onClick: () => void;
    children: React.ReactNode;
} | null;
